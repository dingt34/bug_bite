# “虫咬识途”本次修改报告

> 修改日期：2026 年 8 月 25 日
> 对应提交：`abc3633`
> 提交说明：`实现社区云端化并修复导航与头像显示`
> GitHub 分支：`dingt34/bug_bite` 的 `main`

## 一、本次修改概述

本次修改主要完成三项工作：将原有本地社群升级为多用户云端社群、修复底部导航栏选中状态异常、修复个人页面微信头像无法正常显示的问题。同时补充云端部署说明、隐私边界和自动化回归测试。

本次提交共修改或新增 43 个文件，代码变更约为新增 1648 行、删除 369 行。提交已成功推送至 GitHub。

## 二、社区云端化

### 1. 修改前的问题

原社群内容主要保存在本机缓存中，不同用户之间无法真正共享帖子、评论、点赞和收藏。个人同步快照中还包含公共社群数据，存在多设备覆盖和公共数据边界不清晰的问题。

### 2. 本次实现

- 新增独立 `community` 云函数，统一处理社群数据读写。
- 新增帖子、评论、互动和举报四类云数据库集合。
- 社群作者身份由云函数获取的微信 `OPENID` 判断，不信任客户端传入的用户 ID。
- 支持帖子列表、详情、发布、点赞、收藏、评论、删除、举报和个人统计。
- 支持搜索、主题筛选、最新/热门排序、下拉刷新、触底加载和失败重试。
- 支持旧版本本地社群数据一次性迁移。
- 将公共社群数据从个人 `user_data` 同步快照中移除。
- 删除账户时同时清理本人云端社群内容和互动数据。

### 3. 云端集合

本次新增以下集合：

- `community_posts`：保存帖子正文、主题、图片、作者摘要和统计信息。
- `community_comments`：保存帖子评论及作者信息。
- `community_reactions`：保存用户对帖子的点赞和收藏状态。
- `community_reports`：保存用户举报记录和审核状态。

原有集合继续保留：

- `users`：保存微信云身份资料。
- `user_data`：保存计划、事件和个人同步数据。

### 4. 权限与内容安全

- 六个集合建议统一设置为“仅管理端可读写”。
- 小程序客户端不直接访问数据库，只能调用云函数。
- 帖子和评论提交时调用微信文本内容安全检查。
- 新增 `community/config.json`，声明 `security.msgSecCheck` 云调用权限。
- 举报内容会写入 `community_reports`，并从举报者的社群列表中隐藏。

图片安全检测和管理员举报审核页面暂未实现，正式运营前仍需补充。

## 三、底部导航栏修复

### 1. 问题表现

用户切换首页、社群和我的页面时，社群图标始终保持绿色高亮，首页和我的图标无法显示正确的选中状态。

### 2. 原因分析

原实现完全依赖微信原生 tabBar 维护选中索引。在调试会话、跨 tab 跳转或页面重新显示后，选中状态没有可靠地与当前页面同步。

### 3. 修复方案

- 将底部导航栏改为自定义 tabBar。
- 新增统一的导航状态同步工具。
- 首页显示时将选中索引设为 `0`。
- 社群显示时将选中索引设为 `1`。
- 我的页面显示时将选中索引设为 `2`。
- 保留原有灰色普通图标、绿色选中图标和文字配色。
- 调整页面底部留白和社群发布按钮位置，避免内容被导航栏遮挡。

### 4. 修复结果

切换页面后只有当前页面的图标和文字会高亮。从个人页的“我的发布”“我的评论”“我的收藏”跳转到社群后，再切换其他 tab，选中状态仍能正确更新。

## 四、个人页头像显示修复

### 1. 问题表现

微信云登录成功后，个人页头像区域可能显示为空白，无法显示用户选择的微信头像。

### 2. 原因分析

云登录保存的是 `cloud://` 云文件标识。个人页此前直接将该标识传给图片组件；当存储权限、临时访问地址或缓存影响图片加载时，头像区域会变成空白。同时页面没有图片加载失败后的回退处理。

### 3. 修复方案

- 个人页显示云头像前，先调用云服务获取临时可访问地址。
- 每次进入个人页时重新解析，避免临时地址过期。
- 增加图片加载错误监听。
- 临时地址获取失败或图片加载失败时，自动显示昵称首字。
- 保持原有头像尺寸、圆形样式和个人页布局不变。

### 4. 修复结果

正常情况下显示用户选择的微信头像；网络、权限或图片文件异常时显示昵称首字，不再出现空白头像框。

## 五、同步与隐私调整

- `syncData` 仅同步计划、最近计划、离线卡片、事件、云文件映射和删除标记等个人数据。
- 帖子、评论、点赞、收藏和举报使用独立公共集合，不参与个人完整快照覆盖。
- 隐私页面明确区分本机数据、个人云备份、公共社群和 AI 临时数据。
- 个人页社群统计改为从云端获取并进行本机短期缓存。
- 删除云端账户时，同时调用社区账户清理操作。

## 六、新增或重点修改文件

### 1. 云函数

- `cloudfunctions/community/index.js`
- `cloudfunctions/community/domain.js`
- `cloudfunctions/community/config.json`
- `cloudfunctions/community/package.json`
- `cloudfunctions/syncData/index.js`

### 2. 小程序端

- `miniprogram/utils/community-cloud.js`
- `miniprogram/utils/cloud-sync.js`
- `miniprogram/utils/cloud-service.js`
- `miniprogram/utils/tab-bar.js`
- `miniprogram/custom-tab-bar/`
- `miniprogram/pages/community/`
- `miniprogram/pages/post-detail/`
- `miniprogram/pages/post-publish/`
- `miniprogram/pages/profile/`

### 3. 测试

- `tests/community-cloud-domain.test.js`
- `tests/community-cloud.test.js`
- `tests/profile-avatar.test.js`
- `tests/tab-bar.test.js`
- 社群、帖子详情、帖子发布、个人同步等原有测试同步更新。

## 七、测试与检查结果

本次修改完成后进行了全量自动检查：

- 32 个测试脚本全部通过。
- 社群云端领域规则测试通过。
- 社群云服务接口测试通过。
- 社群列表、帖子详情和帖子发布页面测试通过。
- 云同步和隐私数据边界测试通过。
- 底部导航选中状态回归测试通过。
- 云头像临时地址解析和加载失败回退测试通过。
- JavaScript 语法检查通过。
- JSON 配置解析检查通过。
- Git 差异格式检查通过。

## 八、GitHub 提交情况

- 提交编号：`abc3633`
- 提交说明：`实现社区云端化并修复导航与头像显示`
- 推送分支：`main`
- 推送范围：`4887536..abc3633`
- 远程状态：`origin/main` 已指向 `abc3633`

## 九、部署与人工验收要求

### 1. 云端部署

在微信开发者工具中：

1. 创建并检查四个 `community_*` 集合。
2. 将六个集合权限设置为“仅管理端可读写”。
3. 根据 `CLOUD_SETUP.md` 配置组合索引。
4. 重新部署 `syncData` 云函数。
5. 部署 `community` 云函数并选择云端安装依赖。
6. 检查 `security.msgSecCheck` 云调用权限是否生效。

### 2. 社群验收

使用两个真实微信账号进行测试：

1. 账号 A 发布帖子，账号 B 能看到。
2. 账号 B 点赞、收藏和评论，账号 A 能看到统计变化。
3. 账号 B 不能删除账号 A 的帖子或评论。
4. 作者能够删除自己的帖子和评论。
5. 举报后云数据库中出现对应举报记录。
6. 客户端直接读取社群集合时被权限规则拒绝。

### 3. 导航和头像验收

1. 清除微信开发者工具缓存并重新编译。
2. 依次点击首页、社群和我的，确认只有当前图标和文字为绿色。
3. 从个人页进入我的发布、我的评论和我的收藏，再切换其他 tab。
4. 微信云登录后进入个人页，确认显示所选头像。
5. 模拟图片加载失败，确认显示昵称首字而不是空白头像。

## 十、仍需完成的部分

- 在真实云环境中部署并验收最新 `community` 和 `syncData` 云函数。
- 使用双账号完成多用户共享和作者权限验证。
- 增加社群图片内容安全检测和管理员举报审核页面。
- 将当前近期候选数据分页升级为真正的游标分页。
- 增加限流、违规处理、消息通知和评论回复。
- 配置 AI Agent ID，并完成文字与图片真机调用验证。
- 修复快速重复提交产生两个相同行前计划的问题。
- 收集、审核判断知识库并接入 Agent 检索。

## 十一、本次修改结论

本次修改将社群从单机缓存功能升级为具备可信用户身份和权限控制的云端多用户功能，并修复了底部导航状态和个人头像显示两个直接影响使用体验的问题。自动测试结果正常，代码已经推送到 GitHub。

当前代码层面的本次目标已经完成；后续重点是云端实际部署、双账号真机验收、图片内容治理和判断知识库建设。
