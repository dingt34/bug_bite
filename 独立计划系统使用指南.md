# 独立计划系统使用指南

## 🎯 解决的问题

**原问题**：创建多个草稿计划时，它们跳转和修改的都是同一个页面/数据，没有实现计划间互相独立的状态。

**解决方案**：为每个计划创建完全独立的数据存储和加载系统。

## 🏗️ 系统架构

### 1. 数据结构
```
planDetails: {
  "plan-1": { 独立计划A的所有数据 },
  "plan-2": { 独立计划B的所有数据 },
  "plan-3": { 独立计划C的所有数据 }
}

routePlans: {
  "plan-2": { 计划B的行程单数据 },
  "plan-3": { 计划C的行程单数据 }
}
```

### 2. 关键字段
- **plan.id**: 每个计划的唯一标识符
- **plan.hasRoutePlan**: 是否有独立的行程单数据
- **独立存储**: 每个计划在`planDetails`中有独立的详情数据

## 🔧 已修改的文件

### 1. `my-plans.js`
- **open函数**：根据计划ID决定跳转到哪个页面
- **数据加载**：修改了跳转逻辑，确保传递正确的ID

### 2. `precheck.js`
- **onLoad函数**：根据ID加载对应的计划详情数据
- **loadPlanData函数**：从`planDetails`加载独立数据
- **savePlanData函数**：保存到对应的计划ID

### 3. `route-plan.js`
- **onLoad函数**：根据ID加载对应的行程单数据
- **loadRoutePlan函数**：从`routePlans`加载独立的行程单
- **saveRoutePlan函数**：保存到对应的计划ID

## 📊 三个独立计划的示例

### 计划A：杭州西湖徒步
```javascript
{
  id: 'plan-1',
  title: '计划A：杭州西湖徒步',
  status: '草稿',
  hasRoutePlan: false,
  // 独立的数据详情
  precheckData: { essentials: ['登山鞋', '水壶', ...] }
}
```

### 计划B：黄山露营
```javascript
{
  id: 'plan-2',
  title: '计划B：黄山露营',
  status: '计划中',
  hasRoutePlan: true,
  // 独立的详情和行程单
  precheckData: { essentials: ['帐篷', '睡袋', ...] },
  routePlan: { name: '黄山三日露营行程', ... }
}
```

### 计划C：张家界探险
```javascript
{
  id: 'plan-3',
  title: '计划C：张家界探险',
  status: '已规划',
  hasRoutePlan: true,
  // 独立的详情和行程单
  precheckData: { essentials: ['探险装备', '绳索', ...] },
  routePlan: { name: '张家界探险五日行程', ... }
}
```

## 🧪 测试步骤

### 第一步：设置测试数据
1. 打开微信开发者工具
2. 在控制台执行 `独立计划系统.js` 文件
3. 确认看到"✅ 独立计划系统设置完成！"消息

### 第二步：测试独立跳转
1. 刷新小程序页面
2. 在"草稿"界面看到三个独立的计划
3. 分别点击每个计划测试：
   - **计划A** → 跳转到 `precheck`（新建行前准备）
   - **计划B** → 跳转到 `route-plan`（编辑行程单）
   - **计划C** → 跳转到 `route-plan`（编辑行程单）

### 第三步：验证数据独立性
1. 在 `precheck` 页面修改计划A的数据
2. 返回后点击计划B，确认看到的是B的数据
3. 修改计划B的数据，确认不影响计划A和C
4. 所有计划都保持各自独立的数据状态

## ⚠️ 重要说明

### 1. 数据保存位置
- **计划列表**：存储在 `wx.getStorageSync('plans')`
- **计划详情**：存储在 `wx.getStorageSync('planDetails')`
- **行程单数据**：存储在 `wx.getStorageSync('routePlans')`

### 2. 创建新计划
当用户创建新计划时，需要：
1. 生成唯一的ID
2. 在 `planDetails` 中创建对应的空数据
3. 在 `plans` 列表中添加新计划

### 3. 删除计划
需要同时删除：
1. `plans` 列表中的计划
2. `planDetails` 中的对应数据
3. `routePlans` 中的行程单数据（如果有）

## 🎯 预期效果

现在您应该能够：
1. ✅ 创建多个独立的草稿计划
2. ✅ 每个计划都有自己独立的数据
3. ✅ 跳转到正确的页面（根据是否有行程单）
4. ✅ 修改一个计划不会影响其他计划
5. ✅ 所有计划保持互相独立的状态

## 🔄 如果需要恢复原始状态
1. 在控制台执行：
```javascript
wx.removeStorageSync('plans');
wx.removeStorageSync('planDetails');
wx.removeStorageSync('routePlans');
```

## 📞 问题排查

如果遇到问题，检查：
1. 是否正确设置了测试数据
2. 页面是否接收到正确的ID参数
3. 本地存储中是否有对应的数据
4. 控制台是否有错误信息

现在您的行程计划系统已经支持完全独立的数据存储了！