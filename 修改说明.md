# 行程界面修改说明

## ✅ 已完成的修改

### 1. 界面结构调整
- **删除"近期"选项卡**：从3个选项卡变为2个
- **历史界面**：只展示在小程序上记录过的行程
- **草稿界面**：记录行前的计划

### 2. 跳转逻辑优化
根据您的需求，修改了点击行程时的跳转逻辑：

#### 📝 草稿行程（行前计划）
- **点击跳转** → `/pages/precheck/precheck`（行前准备页面）
- **适用**：所有草稿计划，包括"即将出发"和"其他计划"

#### 📋 历史行程（已完成行程）
- **有事件发生** → `/pages/event-detail/event-detail`（事件详情页面）
- **无事件发生** → `/pages/precheck-result/precheck-result`（行前准备结果页面）
- **判断依据**：行程数据中的 `hasEvents` 字段

### 3. 数据结构更新
为支持新的跳转逻辑，需要在行程数据中添加：
```javascript
// 历史行程需要添加
hasEvents: true/false  // 是否有事件发生

// 草稿行程需要添加
isUpcoming: true/false  // 是否是即将出发的行程
```

### 4. 代码修改
#### JavaScript (`my-plans.js`)
1. 修改 `tabs` 数组：`['历史', '草稿']`
2. 修改 `open` 函数：根据tab和`hasEvents`决定跳转页面
3. 添加数据预处理逻辑：分离出`upcomingDraft`和`otherDrafts`

#### WXML (`my-plans.wxml`)
1. 重构草稿界面：分为"即将出发"和"其他计划"
2. 使用预处理的数据展示，避免复杂表达式

## 🧪 测试方法
1. 使用 `测试数据-带事件标识.js` 设置测试数据
2. 在微信开发者工具控制台执行该文件
3. 刷新页面测试不同跳转逻辑

## 🔍 跳转逻辑验证
### 测试场景1：草稿行程
- 点击"即将出发"的行程 → 跳转到 `precheck` 页面
- 点击"其他计划"的行程 → 跳转到 `precheck` 页面

### 测试场景2：历史行程
- 点击"有事件"的历史行程 → 跳转到 `event-detail` 页面
- 点击"无事件"的历史行程 → 跳转到 `precheck-result` 页面

## 📁 修改的文件
1. `miniprogram/pages/my-plans/my-plans.js`
2. `miniprogram/pages/my-plans/my-plans.wxml`
3. `miniprogram/pages/my-plans/my-plans.wxss`

## ⚠️ 注意事项
1. 实际数据中需要包含 `hasEvents` 字段来判断是否有事件
2. 如果现有数据结构中没有此字段，需要更新数据获取逻辑
3. 可以根据实际需求调整默认跳转页面