# “虫咬识途”小程序修改报告

> 报告日期：2026 年 8 月 25 日
> 项目名称：虫咬识途——节肢动物接触安全管理助手
> GitHub 仓库：`dingt34/bug_bite`
> 当前分支：`main`
> 最新推送提交：`abc3633 实现社区云端化并修复导航与头像显示`

## 一、修改目标

本阶段围绕“行前预防—接触后判断—事件记录—复查跟踪—社群交流—AI 建议—个人数据同步”完整流程进行完善，主要目标如下：

1. 优化安全判断规则与交互，减少重复判断和不合理的紧急分级。
2. 完善行前计划、事件图片、复查记录和个人数据管理。
3. 将本地社群升级为基于微信云开发的多用户公共社群。
4. 接入微信云登录、个人数据同步、云存储和内容安全能力。
5. 将首页昆虫识别入口升级为可发送图片和个人记录的 AI 建议助手。
6. 修复底部导航高亮和个人页头像显示问题。

当前版本已具备阶段演示和功能验收条件，代码已推送至 GitHub。正式发布前仍需完成云端部署复核、双账号真机验收、图片内容审核和判断知识库建设。

## 二、主要修改内容

### 1. 接触后安全判断

- 删除接触后指导中的重复判断内容，避免用户重复作答。
- 将“多个部位被叮咬”移出单独的紧急求助范围，不再仅以身体部位数量判断急症。
- 将“大量多处”和“快速加重”移动到紧急求助判断中。
- 突出呼吸困难、意识异常、口唇舌喉肿胀和症状快速加重等危险信号。
- 保留“建议不替代医疗诊断”的安全提示和紧急联系入口。

### 2. 行前准备与计划

- 调整行前准备选项，使目的地、活动、环境、住宿、同行人员和装备选择更符合实际出行场景。
- 支持在同一计划中选择多个地点。
- 完成行前准备后提供明确的返回路径。
- 将结果页“重新填写”改为“继续计划”。
- 优化计划列表、最近计划和离线安全卡之间的跳转关系。
- 删除离线缓存卡中重复的“继续计划”入口。

### 3. 事件记录与复查

- 修复事件记录未保存图片的问题。
- 将图片输入拆分为“虫体图片”和“伤口图片”两个独立入口。
- 在事件详情中分别展示两类图片，并支持图片预览。
- 优化“更新症状与复查”选项，使症状范围、变化趋势和危险信号描述更科学。
- 支持保存多次复查记录，并在出现危险信号时升级风险提示。
- 将个人页最近事件改造成与最近计划一致的摘要卡片。
- 点击最近事件可进入全部事件列表，并删除重复的“全部事件”入口。

### 4. 多用户云端社群

- 实现帖子发布、列表、详情、搜索、筛选、排序、点赞、收藏和评论。
- 增加评论删除、帖子删除、举报和对举报者隐藏等操作。
- 增加“我的发布”“我的评论”“我的收藏”统计与筛选，顺序统一为“发布—评论—收藏”。
- 从个人页点击对应统计项，可直接进入社群相应筛选页面。
- 增加云端加载状态、空状态、失败重试、下拉刷新和触底加载。
- 支持将旧版本本地帖子、评论和互动记录一次性迁移到云端。
- 新增独立 `community` 云函数，由云函数统一处理公共社群读写。
- 使用微信云上下文中的可信 `OPENID` 判断作者身份和操作权限。
- 公共社群数据不再放入个人 `user_data` 完整快照，避免跨设备同步互相覆盖。

社群使用以下四个独立集合：

- `community_posts`：帖子。
- `community_comments`：评论。
- `community_reactions`：点赞与收藏。
- `community_reports`：举报记录。

### 5. 个人页面

- 重构个人页信息结构，划分为身份与同步、安全记录、社群参与、最近计划、最近事件和管理设置。
- 增加计划、事件、发布、评论和收藏数量统计。
- 最近计划与最近事件采用统一卡片结构。
- 收藏入口点击后自动进入“我的收藏”。
- 显示微信云身份、本地体验身份和云同步状态。
- 增加隐私说明、本机数据管理和云端数据删除入口。

### 6. 微信登录、头像和个人云同步

- 实现微信头像选择、昵称填写和微信云身份登录。
- 新增 `login` 云函数，使用可信 `OPENID` 创建或更新 `users` 用户资料。
- 新增 `syncData` 云函数，在 `user_data` 保存计划、事件和图片映射等个人数据。
- 本地保存优先，云同步失败不会阻止用户继续记录。
- 本地图片同步前上传至云存储，数据库保存云文件 ID。
- 增加删除标记，避免其他设备重新恢复已删除的数据。
- 支持清除本机数据和删除云端个人数据。
- 修复个人页云头像不显示的问题：进入个人页时将 `cloud://` 文件标识转换为临时可访问地址。
- 增加头像加载失败回退；无法加载图片时显示昵称首字，不再出现空白头像框。

当前配置的云环境 ID 为：`cloudbase-d1ggskwel61500f0e`。

### 7. AI 建议助手

- 将首页“昆虫识别”替换为“AI 建议助手”。
- 支持文字连续对话、快捷问题和手动发送图片。
- 图片输入分为虫体图片和伤口图片。
- 增加“一键发送我的记录”，发送前由用户选择具体计划或事件。
- 默认不选择任何记录，只有用户明确勾选的内容才进入本轮 AI 请求。
- 单次最多选择 8 条记录，避免上下文过长。
- 未选择的记录和历史图片不会被自动发送。
- AI 回复持续展示危险信号和医疗安全提示。

### 8. 底部导航栏

- 修复切换页面时社群始终高亮、首页和我的图标不高亮的问题。
- 将底部导航改为可显式控制选中状态的自定义 tabBar。
- 首页、社群和我的页面显示时分别同步选中索引 `0`、`1`、`2`。
- 保留原有灰色普通图标、绿色高亮图标和文字配色。
- 调整三个 tab 页底部留白及社群发布按钮位置，避免被自定义导航栏遮挡。

### 9. 隐私与数据边界

- 个人计划、事件和图片映射保存在个人同步集合中。
- 公共帖子、评论、互动和举报保存在独立社群集合中。
- 数据库集合建议设置为仅管理端可读写，客户端只能通过云函数访问。
- AI 发送个人记录前要求用户主动选择，减少不必要的数据披露。
- 隐私页面区分本机数据、个人云备份、公共社群内容和 AI 临时数据。

## 三、云端结构与安全设计

### 1. 云函数

- `login`：确认微信身份并保存用户资料。
- `syncData`：上传、拉取和删除个人同步数据。
- `community`：处理社群列表、详情、发布、互动、评论、删除、举报、统计和账户清理。

### 2. 数据库集合

- `users`
- `user_data`
- `community_posts`
- `community_comments`
- `community_reactions`
- `community_reports`

六个集合均建议设置为“仅管理端可读写”。小程序客户端不直接读写数据库，由云函数在可信环境中处理权限。

### 3. 社群内容安全

- 帖子和评论提交时调用微信文本内容安全检查。
- `community/config.json` 已声明 `security.msgSecCheck` 云调用权限。
- 举报记录写入 `community_reports`，并对举报者隐藏相应内容。
- 图片异步安全检测和管理员审核后台尚未实现。

## 四、问题修复说明

### 1. 社群导航始终高亮

原实现完全依赖原生 tabBar 维护选中索引，在调试会话和跨 tab 跳转后出现社群索引未更新。现改为页面显示时显式同步状态，并增加自动测试依次验证首页、社群和我的选中状态。

### 2. 个人页头像为空

云登录保存的是 `cloud://` 文件标识，个人页此前直接交给图片组件显示；当访问权限或缓存影响加载时会出现空白。现增加临时 URL 解析及图片错误回退。

### 3. 事件图片未保存

事件结构已增加虫体图片和伤口图片字段，创建事件、详情显示、个人同步和隐私统计均使用统一数据结构。

## 五、验证结果

截至本报告更新时：

- 32 个测试脚本全部通过。
- 社群云端领域规则、云端客户端接口及页面交互测试通过。
- 微信登录、云同步、隐私和个人页统计测试通过。
- 底部导航选中状态回归测试通过。
- 云头像临时地址解析和失败回退测试通过。
- AI 对话、图片输入、记录选择和上下文隔离测试通过。
- 行前准备、风险判断、事件记录和复查测试通过。
- JavaScript 语法检查通过。
- JSON 配置解析检查通过。
- Git 差异格式检查通过。

自动测试不能替代微信开发者工具编译、真机授权、云函数调用和多设备视觉验收。

## 六、GitHub 更新情况

本阶段代码已提交并推送至 GitHub：

- 仓库：`dingt34/bug_bite`
- 分支：`main`
- 提交：`abc3633`
- 提交说明：`实现社区云端化并修复导航与头像显示`
- 推送范围：`4887536..abc3633`

报告更新完成后，本报告文件本身会形成新的本地修改，需要在下一次提交时再次推送。

## 七、仍需完成的部分

### A. 上线前必须完成

#### 1. 部署最新云函数

在微信开发者工具中重新部署：

- `login`
- `syncData`
- `community`

其中 `community` 应选择云端安装依赖，并确认 `security.msgSecCheck` 权限生效。

#### 2. 创建集合、权限和索引

确认六个集合名称完全正确，尤其是 `users` 为复数。建议权限统一设置为“仅管理端可读写”。

需要为社群集合配置常用组合索引，包括帖子状态与时间、作者与状态、评论帖子与时间、互动用户与帖子、举报用户与帖子等。

#### 3. 双账号验收云端社群

至少使用两个真实微信账号验证：

1. 账号 A 发布帖子，账号 B 可以看到。
2. 账号 B 点赞、收藏和评论，账号 A 可以看到统计变化。
3. 账号 B 不能删除账号 A 的帖子或评论。
4. 作者可以删除自己的帖子和评论。
5. 举报记录能够进入 `community_reports`。
6. 小程序客户端直接访问数据库集合时应被拒绝。

#### 4. 完成 AI Agent 配置

当前 `AI_BOT_ID` 仍为空。需要在云开发 AI 控制台创建 Agent，填写 Bot ID，并真机验证文字、虫体图片、伤口图片、临时上传和清理流程。

#### 5. 真机全流程验收

建议依次验证微信登录、头像显示、多地点计划、事件双图片、复查、危险判断、社群发布互动、个人页筛选跳转、AI 选择记录和跨设备同步。

### B. 已定位但尚未修复

#### 6. 重复行前计划

同一表单快速或重复提交时会创建两个内容相同但 ID 不同的计划。原因是提交函数尚未设置提交锁，计划 ID 又使用时间生成。

后续应增加：

- 提交中状态和按钮禁用。
- 同一页面生命周期内的重复提交拦截。
- 必要时基于表单内容与时间窗口进行幂等校验。
- 对应的双击提交回归测试。

### C. 正式运营前建议完成

#### 7. 建设判断知识库并接入 Agent

- 收集可信的虫咬处置、危险信号和行前防护资料。
- 为知识记录来源、地区、人群、日期和审核版本。
- 将页面中的判断规则逐步抽离为可维护知识库。
- Agent 检索知识库后再生成建议，并展示来源和不确定性。
- 建立固定评估案例，重点检查急症漏报、过度报警和错误物种判断。
- 高风险规则须由医学或公共卫生专业人员审核。

#### 8. 内容治理

- 增加社群图片安全检测。
- 建设管理员举报审核页面。
- 增加违规内容隐藏、删除和申诉机制。
- 增加发布、评论和互动频率限制。

#### 9. 数据同步与性能

- 将个人完整快照同步升级为记录级增量同步，降低多设备互相覆盖风险。
- 将社群近期 100 条候选读取升级为真正的游标分页。
- 增加云函数耗时、错误率、AI 调用量和费用监控。

#### 10. 产品体验

- 增加社群评论回复、消息通知和未读提醒。
- 增加帖子编辑、草稿和作者主页。
- 增加 AI 停止生成、重新生成、失败重试和聊天历史清理。
- 完成全面屏、大字体、低端安卓机及无障碍验收。

## 八、建议后续实施顺序

1. 在微信开发者工具中部署最新 `syncData` 和 `community` 云函数。
2. 检查六个数据库集合的名称、权限和索引。
3. 使用双账号验收多用户社群及作者权限。
4. 在真机验收底部导航、头像、计划、事件、复查和 AI 助手。
5. 修复重复计划提交问题并增加回归测试。
6. 配置 AI Agent，完成文字与图片调用验收。
7. 增加图片内容安全和举报审核能力。
8. 建设并审核判断知识库，再接入 Agent 检索判断。

## 九、阶段结论

当前版本已完成主要业务闭环、微信云登录、个人数据同步、多用户云端社群代码、AI 建议入口以及个人页和导航体验修复。代码已经推送到 GitHub，自动测试全部通过。

正式发布的核心差距集中在四点：云端资源部署与双账号验收、重复计划提交修复、AI Agent 与图片审核配置、经过专业审核且可追溯的判断知识库。
