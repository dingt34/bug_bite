# “虫咬识途”小程序修改报告

> 报告日期：2026 年 8 月 25 日  
> 项目目录：`虫咬识途`  
> 当前 GitHub 主分支最近一次已推送提交：`4887536 完善社群与个人页并新增AI建议助手`

## 一、项目修改概况

本轮修改围绕“行前预防—接触后判断—事件记录—复查跟踪—社群交流—AI 建议—个人数据同步”完整流程展开。现阶段已经完成主要页面和本地业务闭环，并接入微信登录、云函数、云数据库同步和云存储。

当前代码已具备演示和阶段验收条件，并已完成多用户云端社区代码；正式上线前仍需部署社区集合和云函数、配置 AI Agent、完善图片审核并完成真机联调。

## 二、已完成的修改

### 1. 接触后安全判断

- 删除接触后指导中重复的判断内容，减少用户重复作答。
- 将“多个部位被叮咬”移出单独的紧急求助范围，避免仅以部位数量判定急症。
- 将“大量多处”和“快速加重”纳入紧急求助判断。
- 调整结果页内容层级，突出呼吸困难、意识异常、口唇舌喉肿胀、症状快速加重等危险信号。
- 保留“建议不替代医疗诊断”的安全提示，并提供紧急联系入口。

### 2. 事件记录与复查

- 修复事件记录图片未保存的问题。
- 将图片输入拆分为“虫体图片”和“伤口图片”两个独立入口。
- 在事件详情中显示已保存图片，并支持后续复查记录。
- 优化“更新症状与复查”选项，使变化趋势和症状描述更加清晰。
- 最近事件改造成与最近计划一致的摘要卡片。
- 点击最近事件可进入全部事件列表。
- 删除个人页中重复的“全部事件”入口。

### 3. 行前准备与计划

- 调整行前准备可选项，使准备内容更贴近真实出行场景。
- 支持一次计划选择多个地点。
- 完成行前准备后增加返回路径，避免用户停留在结果页无法退出。
- 将“重新填写”改为“继续计划”，使按钮含义与业务流程一致。
- 优化计划列表、最近计划和离线安全卡之间的跳转关系。
- 删除离线缓存卡中的“继续计划”选项，减少重复入口。

### 4. 社群功能

- 增加帖子发布、详情查看、点赞、收藏和评论功能。
- 增加“我的评论”统计及筛选。
- 社群个人分类按“发布—评论—收藏”顺序排列。
- 从个人页点击发布、评论或收藏统计，可自动进入社群对应筛选页面。
- 增加关键词搜索、接触类型筛选和最新/热门排序。
- 增加帖子评论列表、评论删除、帖子删除和云端举报。
- 优化社群列表、帖子详情和发布页的布局、信息密度和空状态。
- 新增独立 `community` 云函数，以及帖子、评论、互动和举报四个公共集合。
- 作者权限由云函数取得的微信 `OPENID` 判断，数据库不开放客户端直接读写。
- 支持云端加载、下拉刷新、触底加载、失败重试和旧版本本机帖子迁移。
- 公共社群内容已从个人 `user_data` 同步快照中移除。

### 5. 个人页面

- 重构个人页结构，分为身份与同步、安全记录、社群数据、最近计划和最近事件等区域。
- 优化登录状态、头像昵称和云同步状态展示。
- 增加发布、评论、收藏统计，并提供对应页面跳转。
- 收藏入口点击后直接进入“我的收藏”。
- 最近计划和最近事件采用统一卡片结构。
- 增加隐私与本机数据管理入口。

### 6. 微信登录和云服务

- 实现微信头像、昵称填写及微信云身份登录流程。
- 新增 `login` 云函数，使用微信云上下文中的可信 `OPENID` 识别用户。
- 新增 `syncData` 云函数，在 `user_data` 中保存用户数据快照。
- 本地数据优先保存；云同步失败不会阻断用户记录。
- 本地图片同步前上传云存储，数据库保存云文件 ID。
- 支持计划、事件和个人图片映射的同步；公共社群数据使用独立集合。
- 增加删除标记，避免已删除计划或离线安全卡被其他设备重新恢复。
- 支持清除本机数据和删除云端个人数据。
- 已配置云环境 ID：`cloudbase-d1ggskwel61500f0e`。

### 7. AI 建议助手

- 将首页“昆虫识别”入口替换为“AI 建议助手”。
- 支持文字连续对话和快捷问题。
- 支持手动添加虫体图片或伤口图片。
- 手动发送的图片临时上传云存储，并在本轮回复后尝试清理。
- 增加“一键发送我的记录”入口。
- 发送记录前弹出选择面板，计划和事件分组显示。
- 默认不选中任何记录，只发送用户明确勾选的文字摘要。
- 未选择的记录不会加入本轮 AI 请求，历史图片不会自动发送。
- 单次最多选择 8 条记录，避免上下文过长。
- AI 回复持续显示医疗安全提示和紧急危险信号。

### 8. 隐私与数据控制

- 增加本机数据摘要，展示计划、事件、帖子、收藏、评论和图片数量。
- 数据导出和云同步范围已覆盖新增评论、举报记录与删除标记。
- 记录发送 AI 前由用户主动选择，降低不必要的数据披露。
- 云数据库建议设置为仅管理员或云函数可读写，客户端不直接访问用户集合。

## 三、验证结果

截至本报告生成时，已完成以下自动检查：

- 28 个测试脚本全部通过。
- AI 对话、AI 记录选择和上下文隔离测试通过。
- 行前准备、风险判断、事件、复查、社群、登录、云同步、隐私和个人页测试通过。
- 小程序 JavaScript 语法检查通过。
- 全部 WXSS 大括号结构检查通过。
- Git 差异格式检查未发现代码格式错误。

仍需注意：自动测试不能替代微信开发者工具编译、真机授权、云函数调用和不同手机尺寸下的视觉验收。

## 四、当前尚未完成或需要确认的部分

### A. 上线前必须完成

#### 1. 提交并更新 GitHub 仓库

最近完成的社群优化、个人页重构、AI 助手和记录选择功能仍处于本地未提交状态。GitHub `main` 分支目前只更新到提交 `17431c5`。

建议操作：完成本轮微信开发者工具验收后，统一提交并推送，提交说明可使用：

`完善社群与个人页并新增AI建议助手`

#### 2. 完成 AI+ 和图片 Agent 配置

当前 `AI_BOT_ID` 仍为空：

- 文字模型代码已经接入，但需要确认云开发 AI+ 已开通并具有调用额度。
- 图片分析必须在云开发控制台创建并启用 Agent。
- 创建后需要把 Agent ID 填入 `miniprogram/config/cloud.js`。
- 需要真机验证虫体图片、伤口图片、临时上传和自动清理是否完整执行。

#### 3. 核对云数据库集合名称和权限

代码使用的集合名称为：

- `users`
- `user_data`

必须确认控制台中是 `users`（复数），不是 `user`。两个集合应限制为云函数或管理员访问，并重新验证登录、上传、拉取和删除云端数据。

#### 4. 微信开发者工具和真机全流程验收

至少需要完整走通以下路径：

1. 微信登录并同步。
2. 创建多地点行程计划。
3. 完成行前准备并返回。
4. 新建带虫体图和伤口图的事件。
5. 添加复查记录并查看图片。
6. 进入危险判断并核对紧急结果。
7. 发布帖子、评论、收藏并从个人页跳转。
8. AI 文字提问、图片提问和选择性发送记录。
9. 换设备或清缓存后拉取云端数据。
10. 删除本机及云端个人数据。

### B. 正式运营前需要开发

#### 5. 部署并验证多用户云端社区

多用户公共社区代码已经完成，包含以下独立数据结构：

- `community_posts`
- `community_comments`
- `community_reactions`
- `community_reports`

上线前仍需在云开发控制台创建这些集合、配置索引、部署 `community` 云函数，并使用两个不同微信账号验证内容互相可见及作者删除权限。

#### 6. 增加内容安全审核

云函数已经对帖子和评论调用微信文本内容安全检查，并把举报保存到云端。正式开放前仍需补齐：

- 图片异步安全检测。
- 举报记录后台审核。
- 违规内容隐藏或删除。
- 用户频率限制和恶意刷帖防护。

当前举报会写入 `community_reports` 并对举报者隐藏内容，但尚未提供管理员审核页面。

#### 7. 建设判断知识库并接入 Agent

当前安全判断仍以小程序内规则和通用大模型提示为主。按照后续规划，需要：

- 收集可信来源的虫咬处置、危险信号和行前防护知识。
- 为每条知识记录来源、适用地区、适用人群、发布日期和审核版本。
- 将判断规则从页面代码中抽离，形成可更新、可追溯的知识库。
- 接入 Agent 检索知识库后再生成建议，并展示信息来源和不确定性。
- 建立固定测试案例，评估漏报急症、过度报警和错误物种判断。
- 由医学或公共卫生专业人员审核高风险判断规则。

在完成专业审核前，AI 不应给出确定虫种、疾病诊断或用药剂量结论。

#### 8. 改进多设备同步冲突处理

当前同步采用个人完整快照，适合早期版本，但两个设备同时编辑时可能发生后上传覆盖先上传。

建议后续按记录保存更新时间，采用增量同步和记录级合并；计划、事件、图片和删除操作分别处理，避免整包覆盖。社群数据已经独立为实时云端操作，不再参与个人快照覆盖。

### C. 建议继续优化

#### 9. AI 功能完善

- 允许用户选择单次复查，而不是发送事件中的最近一次复查摘要。
- 增加“本轮已发送哪些数据”的查看入口。
- 增加停止生成、重新生成和网络失败重试。
- 明确是否保存聊天历史，并提供清除按钮。
- 增加 AI 调用次数、失败率和费用监控。

#### 10. 社群体验完善

- 将当前最多读取近期 100 条候选内容升级为基于游标的不限量分页。
- 增加消息通知、评论回复和未读提醒。
- 热门排序改为结合时间衰减，避免旧内容长期置顶。
- 增加作者主页、帖子编辑和草稿保存。

#### 11. 无障碍与视觉适配

- 在低端安卓机、全面屏和大字体模式下检查底部弹层与输入框。
- 减少仅依赖颜色表达选择状态的情况。
- 检查按钮点击区域、文字对比度和屏幕阅读器标签。

#### 12. 清理旧功能代码

首页已经不再使用原“昆虫识别”页面，但相关文件和测试仍保留。确认不再需要旧入口后，可以删除未注册页面及旧识别逻辑，减少维护成本；如果未来仍需使用，应改为 AI 助手中的一个受限工具，而不是独立给出确定识别结果。

## 五、建议后续实施顺序

1. 在微信开发者工具中验收当前页面和业务流程。
2. 创建并验证 `users`、`user_data` 和四个 `community_*` 集合及权限。
3. 开通 AI+、配置 Agent ID，完成文字与图片真机测试。
4. 部署 `community` 云函数，使用两个微信账号完成多用户验收。
5. 提交并推送当前全部修改到 GitHub。
6. 接入图片内容安全和举报审核后台。
7. 收集并审核判断知识库，接入 Agent 检索。
8. 完成多设备冲突、性能、监控和正式发布前测试。

## 六、阶段结论

当前版本已经完成主要交互、个人使用闭环和多用户云端社区代码。正式发布的核心差距集中在三点：云端社区实际部署与双账号验收、经过审核的判断知识库、AI 与图片内容安全的真机验证。
