<safety-nav current="2" bind:stepchange="onStepChange" />
<view class="page">
  <view class="head">
    <view class="head-title">发生了哪类接触？</view>
    <view class="head-sub">选择最接近的一项，不确定也可以继续</view>
  </view>

  <view class="type-card" wx:for="{{types}}" wx:key="key" bindtap="select" data-key="{{item.key}}">
    <view class="type-icon">{{item.icon}}</view>
    <view class="type-body">
      <view class="type-name">{{item.name}}</view>
      <view class="type-desc">{{item.desc}}</view>
    </view>
    <view class="type-arrow">→</view>
  </view>

  <view class="tip">接触类型用于选择后续问题，不代表虫种或疾病诊断。</view>
</view>
