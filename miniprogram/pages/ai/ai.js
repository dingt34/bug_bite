const nav=require('../../utils/nav');const cloud=require('../../utils/cloud');
Page({data:{messages:[{role:'ai',text:'我可以帮你解释行前清单、整理事件摘要，也能对照图鉴特征，但不会判断皮损疾病。'},{role:'user',text:'我准备去丽水徒步，路线经过林地和水边，需要重点准备什么？'},{role:'ai',text:'优先准备包脚鞋、长袖长裤和驱避用品。近水与草木茂密路段建议减少停留，并把离线安全卡提前保存到手机。'}],input:'',sending:false},onShow(){nav.syncTab(this,1);},danger(){wx.navigateTo({url:'/pages/danger/danger?source=ai'});},input(e){this.setData({input:e.detail.value});},send(){const t=this.data.input.trim();if(!t||this.data.sending)return;const before=[...this.data.messages,{role:'user',text:t}];this.setData({messages:before,input:'',sending:true});cloud.call('aiAssistant',{messages:before.map(item=>({role:item.role==='ai'?'assistant':'user',content:item.text}))},{timeout:22000}).then(result=>this.setData({messages:[...before,{role:'ai',text:result.answer}]})).catch(()=>this.setData({messages:[...before,{role:'ai',text:'AI 助手暂时未连接。若症状正在加重，请先进入安全判断；其他功能仍可正常使用。'}]})).finally(()=>this.setData({sending:false}));}});
