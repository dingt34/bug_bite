<view class="custom-nav" style="padding-top: {{statusBarHeight}}px;">
  <view class="nav-row">
    <view class="home-action" bindtap="goHome" role="button" aria-label="返回{{returnLabel}}">
      <text class="home-arrow">‹</text>
    </view>
    <view class="nav-title">{{currentTitle}}</view>
    <view class="nav-placeholder"></view>
  </view>
</view>

<view class="safety-progress" aria-label="当前为第{{current}}步，共4步">
  <view class="progress-meta">
    <text>第 {{current}} 步，共 4 步</text>
    <text>{{currentTitle}}</text>
  </view>
  <view class="progress-track"><view class="progress-value" style="width: {{progressPercent}}%;"></view></view>
  <view class="progress-labels">
  <view
    wx:for="{{steps}}"
    wx:key="step"
    class="safety-step {{item.step < current ? 'step-done' : (item.step === current ? 'step-current' : 'step-future')}}"
    bindtap="onStepTap"
    data-step="{{item.step}}"
  >
    <view class="step-name">{{item.name}}</view>
  </view>
  </view>
</view>
