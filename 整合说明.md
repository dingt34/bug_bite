# 虫咬识途整合说明

本目录以 `Desktop/main/bug_bite-main` 为完整项目底座，整合了 `虫咬识途V8` 中针对 Figma 第 13–16 页持续优化的交互。两个源目录均未修改。

## 已整合内容

- 我的事件：待复查/全部记录真实筛选、统一事件图标和状态样式、搜索与风险筛选、手动同步状态。
- 事件详情：真实时间线、复查时间自定义、图片追加、就医摘要、AI 摘要入口、事件及关联复查删除。
- 复查更新：多选项醒目边框、“其他表现”主观描述、复查草稿、图片记录和复查时间线追加。
- AI 助手：空白会话、参考问题、行程/事件选择、小卡片与复查时间线、发送后清空选择、用户头像、固定输入区域和回复保存。
- 演示数据：启动时清理固定的丽水、杭州及演示事件，不再展示给真实用户。

## AI 接入

底部导航的 AI 页面调用 `cloudfunctions/aiAssistant`。该云函数先从 `knowledge-base` 检索相关对象，再调用千问生成回答。部署和环境变量配置见 `CLOUD_SETUP.md`。

## 云端注意事项

事件同步需要部署 `userData`、`deleteData`、`reminder`；AI 需要部署 `aiAssistant`。数据库集合和环境变量必须按 `CLOUD_SETUP.md` 创建，否则本地记录仍可使用，但云同步或 AI 回复会暂时不可用。
