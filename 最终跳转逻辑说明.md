# 最终跳转逻辑说明

## ✅ 已完成修改

### 1. 界面结构调整
- **删除"近期"选项卡**：从3个选项卡变为2个
- **历史界面**：只展示在小程序上记录过的行程
- **草稿界面**：记录行前的计划，分为新建草稿和已有计划

### 2. 跳转逻辑（最终版本）

#### 📝 历史行程
- **点击跳转** → `/pages/precheck-result/precheck-result`
- **用途**：查看当时填写的行前准备数据和选择（只读模式）

#### 📋 草稿行程（分两种情况）

##### 情况1：新建的草稿（还没有行程单）
- **标识**：`hasRoutePlan: false`
- **点击跳转** → `/pages/precheck/precheck`
- **用途**：填写/编辑行前准备（可编辑模式）
- **适用**：新建的计划，还没有生成行程单

##### 情况2：已有的计划（已经有行程单）
- **标识**：`hasRoutePlan: true`
- **点击跳转** → `/pages/route-plan/route-plan`
- **用途**：查看/编辑已有的行程单
- **提供**：进一步编辑的通道
- **适用**：已经规划过的计划，有详细的行程单

### 3. 代码修改总结

#### JavaScript (`my-plans.js`)
1. **tabs数组**：修改为 `['历史', '草稿']`
2. **默认tab**：设置为 `'历史'`
3. **open函数**：智能跳转逻辑
   ```javascript
   if (tab === '草稿') {
     const draftPlan = this.data.draftPlans.find(plan => plan.id === id);
     if (draftPlan) {
       if (draftPlan.hasRoutePlan) {
         // 已有的计划：跳转到行程单页面
         wx.navigateTo({ url: '/pages/route-plan/route-plan?id=' + id });
       } else {
         // 新建的草稿：跳转到行前准备页面
         wx.navigateTo({ url: '/pages/precheck/precheck?id=' + id });
       }
     }
   } else if (tab === '历史') {
     wx.navigateTo({ url: '/pages/precheck-result/precheck-result?id=' + id });
   }
   ```

#### WXML (`my-plans.wxml`)
1. **历史界面**：显示已完成行程卡片
2. **草稿界面**：分为"即将出发"和"其他计划"两部分
3. **状态标签**：添加"已有行程单"标识

#### CSS (`my-plans.wxss`)
1. **选项卡样式**：从3列改为2列网格
2. **新增样式**：状态标签、空状态页面等

### 4. 数据结构更新

```javascript
// 新建的草稿
{
  id: 'new-draft-1',
  title: '新建的川西之旅',
  status: '草稿',
  hasRoutePlan: false, // 关键字段：没有行程单
  // ...其他字段
}

// 已有的计划
{
  id: 'existing-plan-1',
  title: '夏季海边露营（已有行程单）',
  status: '已规划',
  hasRoutePlan: true, // 关键字段：有行程单
  routePlanName: '海边露营详细行程',
  // ...其他字段
}
```

## 🧪 测试方法

### 测试数据
使用 `测试数据.### 最终跳转逻辑.js`：
1. 复制代码到微信开发者工具控制台执行
2. 包含：2个历史行程 +"2个新建草稿 + 2个已有计划

### 测试步骤
1. **历史界面测试**
   - 点击任意历史行程
   - 应跳转到 `/pages/precheck-result/precheck-result`
   - 查看历史行前准备数据

2. **草稿界面测试 - 新建草稿**
   - 点击"新建的川西之旅"或"春节云南行计划"
   - 应跳转到 `/pages/precheck/precheck`
   - 进入行前准备编辑页面

3. **草稿界面测试 - 已有计划**
   - 点击"夏季海边露营（已有行程单）"或"秋季摄影之旅（已有行程单）"
   - 应跳转到 `/pages/route-plan/route-plan`
   - 查看/编辑已有的行程单

## 📱 页面功能说明

### `precheck` 页面
- **用途**：填写/编辑行前准备
- **模式**：可编辑
- **适用**：新建草稿（`hasRoutePlan: false`）

### `precheck-result` 页面
- **用途**：查看历史行前准备数据
- **模式**：只读
- **适用**：历史行程

### `route-plan` 页面
- **用途**：查看/编辑行程单
- **模式**：可编辑
- **提供**：进一步编辑的通道
- **适用**：已有计划（`hasRoutePlan: true`）

## ⚠️ 注意事项

1. **数据字段**：确保行程数据包含 `hasRoutePlan` 字段
2. **页面兼容性**：
   - `precheck`页面需要支持新建行前准备
   - `route-plan`页面需要能根据ID加载对应的行程单
3. **用户体验**：用户清楚知道新建草稿和已有计划的区别

## 📁 修改的文件

1. `miniprogram/pages/my-plans/my-plans.js` - 核心跳转逻辑
2. `miniprogram/pages/my-plans/my-plans.wxml` - 界面结构调整
3. `miniprogram/pages/my-plans/my-plans.wxss` - 样式优化

## 🔄 总结
现在您的行程界面已经按照需求修改完成：
 | 行程类型 | 界面 | 跳转页面 | 用途 |
|----------|------|----------|------|
| 历史行程 | 历史界面 | precheck-result | 查看历史行前准备 |
| 新建草稿 | 草稿界面 | precheck | 新建行前准备 |
| 已有计划 | 草稿界面 | route-plan | 编辑行程单 |

实现了清晰的区分和符合需求的跳转逻辑！